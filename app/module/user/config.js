/**
 * Config
 */
export const config = {
    rest: {
        resources : {
            user : {
                name: "user"
            },
            recoverPassword : {
                name: "recover-password"
            },
            resetPassword: {
                name: "reset-password"
            },
            activationCode: {
                name: "activation-code"
            }
        }
    }
};